package edu.fiuba.algo3.modelo;

import java.util.List;

//Util si usamos una clase separada para esta logica
//Deberia implementar Bonus
public class ExclusividadPuntaje {
    /*
    @Override
    public void aplicarBonus(List<Respuesta> respuestas) {
        boolean exclusiva = respuestas.get(0).esCorrecta() ^ respuestas.get(1).esCorrecta(); //esCorrecta1 XOR esCorrecta2
        if (exclusiva) {
            for (Respuesta respuesta : respuestas) {
                if (respuesta.esCorrecta()) {
                    respuesta.aplicarExclusividad(factorExclusividad);
                }
            }
        }
    }*/
}
